<%= form_with(model: writer) do |form| %>
  <% if writer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(writer.errors.count, "error") %> prohibited this writer from being saved:</h2>

      <ul>
        <% writer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :profile, style: "display: block" %>
    <%= form.text_field :profile %>
  </div>

  <div>
    <%= form.label :books, style: "display: block" %>
    <%= form.text_field :books %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
